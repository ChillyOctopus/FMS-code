package Services;

public class Register {

    /**
     * The beginning for the user, creates generations and user row in db. Will immediately log user in.
     * @param userID the userID we are registering
     * @return an authtoken related to the user
     */
    public String userRegister(String userID){
        String responseBody = "";
        return responseBody;
    }
}
