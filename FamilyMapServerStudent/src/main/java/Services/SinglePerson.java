package Services;

import Models.Person;

public class SinglePerson {

    /**
     * A person, by ID, we are finding in relation to the userID
     * @param personID the person we are trying to query
     * @param userID the user this person is supposedly associated with
     * @return the Person or null if we didn't find them
     */
    public Person find(String personID, String userID){
        Person p = null;
        return p;
    }
}
